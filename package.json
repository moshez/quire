{
  "name": "quire",
  "version": "0.1.0",
  "description": "PWA e-reader with ATS2/WASM and generic JS bridge",
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "serve": "serve ."
  },
  "devDependencies": {
    "fake-indexeddb": "^6.2.5",
    "jsdom": "^24.0.0",
    "serve": "^14.2.0",
    "vitest": "^1.6.0"
  }
}
